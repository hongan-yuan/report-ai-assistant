# OpenAI 兼容 API（OpenAI / DeepSeek / 通义 / Ollama 等）
OPENAI_BASE_URL=https://api.deepseek.com/v1
OPENAI_MODEL=deepseek-v4-flash

# 默认检测报告目录（前端也可覆盖）
REPORT_DIRECTORY=D:\TestReport

# 单次分析最多纳入的文件数、每个文件最大字符数
MAX_FILES=50
MAX_CHARS_PER_FILE=12000
